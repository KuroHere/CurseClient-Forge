package com.curseclient.client.utility.render

enum class DockingH(val modifier: Double) {
    LEFT(0.0),
    CENTER(0.5),
    RIGHT(1.0)
}

enum class DockingV(val modifier: Double) {
    TOP(0.0),
    CENTER(0.5),
    BOTTOM(1.0)
}
